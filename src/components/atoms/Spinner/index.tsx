import { PulseLoader } from "react-spinners";
import PALETTE from "../../../constants/palette";

const Spinner = () => {
  return <PulseLoader size={10} color={PALETTE.BRAND500} />;
};

export default Spinner;
